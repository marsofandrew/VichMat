//
// Created by marsofandrew on 12.02.18.
//

#ifndef LABS_SQUAREMATRIX_HPP
#define LABS_SQUAREMATRIX_HPP

#include "Matrix.hpp"

/*
template<typename T, const unsigned long size>
class SquareMatrix : public Matrix<T, size, size>
{
public:


  SquareMatrix(const T (&matrix)[size][size]);

  SquareMatrix(const SquareMatrix<T, size> &lhs);

  SquareMatrix(const Matrix<T, size, size> &matrix);

  double countCond() const;

  SquareMatrix<T,size> getInverseMatrix() const;

private:
  SquareMatrix<T,size> inverseMatrix_;
};





template<typename T, const unsigned long size>
SquareMatrix<T, size>::SquareMatrix(const T (&matrix)[size][size]):
SquareMatrix<T,size>(Matrix{matrix})
{

}

template<typename T, const unsigned long size>
SquareMatrix<T,size>::SquareMatrix(const Matrix<T, size, size> &matrix):
  Matrix(matrix)
{

}
*/
#endif //LABS_SQUAREMATRIX_HPP
